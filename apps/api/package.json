{
  "name": "api",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "remove": "serverless remove",
    "clean:unix": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "clean:win": "if exist .turbo rmdir /s /q .turbo && if exist node_modules rmdir /s /q node_modules && if exist dist rmdir /s /q dist",
    "build": "tsc",
    "deploy": "serverless deploy",
    "dev": "tsc && concurrently \"yarn run tsc-watch\" \"nodemon dist/devServer.js\"",
    "tsc-watch": "tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1",
    "express": "^4.18.2",
    "serverless-http": "^3.1.1",
    "ui": "*",
    "utils": "*"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20",
    "serverless-plugin-monorepo": "^0.11.0",
    "serverless-plugin-typescript": "^2.1.4",
    "ts-node": "^10.9.1",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
